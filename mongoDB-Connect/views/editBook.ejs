<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <div align="center">
        <h2>Edit Book Details</h2>
        <form method="post" action="/update-book/<%= book.id %>" enctype="multipart/form-data">
            <table>
                <tr>
                    <td>Title: </td>
                    <td><input type="text" name="title" value="<%= book.title %>" /></td>
                </tr>
                <tr>
                    <td>Price: </td>
                    <td><input type="number" name="price" value="<%= book.price %>" /></td>
                </tr>
                <tr>
                    <td>Quantity: </td>
                    <td><input type="number" name="quantity" value="<%= book.quantity %>" /></td>
                </tr>
                <tr>
                    <td>Category</td>
                    <td>
                        <select name="category">
                            <option value="">Select Category</option>
                            <option value="Education" <% if(book.category == "Education") { %>selected <% } %>  >Education</option>
                            <option value="Entertainment" <% if(book.category == "Entertainment") { %>selected <% } %>>Entertainment</option>
                            <option value="Comedy" <% if(book.category == "Comedy") { %>selected <% } %> >Comedy</option>
                        </select>
                    </td>
                </tr>
                <tr>
                    <td>Description</td>
                    <td><textarea name="description" ><%= book.description %></textarea></td>
                </tr>
                <tr>
                    <td>Upload Image</td>
                    <td><input type="file" name="productImg" />
                        <img src="<%= book.productImg %>" height="100" width="100"/>
                    </td>
                </tr>
                <tr>
                    <td></td>
                    <td><input type="submit" value="Edit Book" /></td>
                </tr>
            </table>
        </form>
    </div>
</body>
</html>